{
  "name": "appscript",
  "version": "1.0.0",
  "description": "Google Apps Script TypeScript project using clasp.",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node scripts/build.mjs",
    "clasp": "clasp",
    "login": "clasp login",
    "push": "npm run build && clasp push --force",
    "open": "clasp open-script",
    "open:web": "clasp open-web-app",
    "deploy": "npm run build && node scripts/deploy.mjs",
    "list:drive:exec": "node scripts/list-drive.mjs",
    "list:drive": "node scripts/list-drive-webapp.mjs",
    "copy:a01": "node scripts/copy-form-webapp.mjs --form a01",
    "inspect:a01": "node scripts/inspect-form-webapp.mjs --form a01",
    "form:a01": "npm run build && npm run push && npm run deploy && npm run copy:a01"
  },
  "keywords": [
    "google-apps-script",
    "clasp",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@google/clasp": "^3.3.0",
    "@types/google-apps-script": "^2.0.8",
    "esbuild": "^0.28.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
